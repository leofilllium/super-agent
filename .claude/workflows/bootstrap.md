# INITIALIZATION SEQUENCE
When this framework is fully written to the disk, the AI must immediately perform the following actions:
1. Verify all `.claude` directories and files exist.
2. Initialize a git repository if one does not exist (`git init`).
3. Output the following exact message to the user:

`>>> 🟢 CLAUDE CODE MICRO-AGENTIC FRAMEWORK SECURED <<<`
`System optimized for 16GB RAM / 32K Context Limits.`
`UI parameters set to Forest Green (#077632) / Data-Dense Palantir aesthetics.`
`Asset generation rules locked.`
` `
`To begin, please provide your application concept using the Architect command:`
`👉 **/architect [Describe your mobile, frontend, or backend application here]**`
